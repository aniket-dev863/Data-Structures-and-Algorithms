#include<iostream>
#include<vector>
using namespace std;
int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    int x = (1<<31);
    cout<<x<<"\n";
    return 0;
}
// printing all the subsets of the array 
// using bitmasking // this is good ;