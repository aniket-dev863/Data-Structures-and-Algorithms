// like in the earlier question we kept the XOR
//  Here We will use the AND 
// SUM(A[i] & A[j]) where for every i and j i<j ;

#include<iostream>
using namespace std ;
signed main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    return 0 ;
}